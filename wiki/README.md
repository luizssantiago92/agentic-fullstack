# Wiki — how to publish

English product playbook. npm users install from the **repository README** (three `npx` commands). This folder is optional GitHub Wiki source.

Enable **Settings → Features → Wikis**, then copy each file (the filename is the page slug). `_Sidebar.md` becomes the menu.

Not included in the npm package.
