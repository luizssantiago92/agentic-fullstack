# Demo

Spec-only example: `.specs/features/demo-login/` (T1 frontend, T2 backend). Not shipped on npm.

```bash
npm run demo:validate
# → node index.js validate-layers demo-login
```

CI (Node 22) runs Seatbelt install + Fullstack install + this check.
